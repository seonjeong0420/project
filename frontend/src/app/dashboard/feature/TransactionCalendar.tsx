import React from 'react';
import { DayPicker } from 'react-day-picker';

type Props = {};

const TransactionCalendar = (props: Props) => {
  return (
    <div>
      <DayPicker />
    </div>
  );
};

export default TransactionCalendar;
